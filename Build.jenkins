pipeline {
  agent any
  stages {
    stage('Cloning Git') {
      steps {
        git 'https://github.com/uzigang16/TodoApp.git'
      }
    }
    stage('Building image') {
      steps{
        sh 'export DOCKER_BUILDKIT=1'
        sh 'docker build .'
      }
    }
  }
}